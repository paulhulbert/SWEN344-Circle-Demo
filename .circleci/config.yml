version: 2
jobs:
  build:
    working_directory: ~/.
    docker:
      - image: circleci/python
    steps:
      - run:
          name: Test this
          command: 'python Main.py'

workflows:
  version: 2
  build_and_test:
    jobs:
      - build